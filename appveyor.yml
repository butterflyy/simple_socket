version: '{build}'

image:
  - Visual Studio 2013
  
environment:
  GYP_MSVS_VERSION: 2013

platform:
  - Win32

configuration:
  - Debug
  - Release


build_script:
  - echo "%cd%"
  - git clone -b develop https://github.com/butterflyy/externals.git  --single-branch --depth=1
  - cd %APPVEYOR_BUILD_FOLDER%
  - msbuild simple_socket.sln /logger:"C:\Program Files\AppVeyor\BuildAgent\Appveyor.MSBuildLogger.dll" /m /verbosity:normal

test_script:
#  - src\client\windows\%CONFIGURATION%\client_tests.exe
#  - src\tools\windows\%CONFIGURATION%\dump_syms_unittest.exe
#  - build\out\Debug\Win32\%CONFIGURATION%\

artifacts:
  - path: '**\*.exe'
#  - path: '**\*.lib'
